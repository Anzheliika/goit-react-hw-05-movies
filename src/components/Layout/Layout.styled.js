import styled from '@emotion/styled';

export const Header = styled.header`
  box-shadow: 0px 0px 8px 6px rgba(234, 187, 242, 1);
  padding: 20px;
  margin: 15px 0;
`;

export const Nav = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 15px;
`;

export const NavItem = styled.li`
  font-size: 26px;
  font-weight: 600;
`;
